import type { UserConfig } from 'vite';

/**
 * Vite configuration for a Tauri window, merged over Tauri-aware defaults.
 */
export declare function createAppViteConfig(options?: UserConfig): UserConfig;

export default createAppViteConfig;
